asyncapi: 2.0.0
info:
  title: My API
  version: '1.0.0'
channels:
  /:
    publish:    
      message:
        $ref: '#/components/messages/testMessage'
components:
  messages:
    testMessage:
      payload:
        # $ref: '#/components/schemas/testSchema'
        $ref: 'refed.yml#/components/schemas/testSchema'
  # schemas:
  #   testSchema:
  #     $ref: 'refed.yml#/components/schemas/testSchema'